# M7.9 P4a scripted-input scenario: "look around in the first chapter
# start".
#
# Project-authored; no bytes here come from any game installation (see
# docs/CLEAN_ROOM.md). Run against ohl_campaign::STARTMAP ("c0a0") by
# xtask/src/combat_smoke.rs.
#
# A line's leading number is a count of *ticks*, each one CAPTURE_STEP
# (1/60 s) of simulated time — not seconds. An earlier revision of this
# file read the counts below as seconds and so covered under two seconds
# of simulation, far too little for the map's opening ride; the counts
# here are sized in ticks deliberately.
#
# Expected fixed log lines (see docs/m79-design.md §7), asserted by the
# harness rather than parsed from this file:
#   present: "Scripted input loaded.", "Scripted input finished.",
#            "The player moved from the spawn point.",
#            "The player is riding a mover."
#   absent:  every other fixed line.
#
# The player spawns standing inside the map's opening tram (a
# func_tracktrain placed on the first node of its own path at spawn) and
# is carried along it: the ride is what makes both "moved from the spawn
# point" and "riding a mover" fire, without this script ever pressing a
# movement key. The four look lines turn the view while riding; the long
# wait covers the rest of the ride.

60 look 0 90
60 look 0 90
60 look 0 90
60 look 0 90
2160 wait
