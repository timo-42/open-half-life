# M9.24 chain-walk route 4: the map the *fourth* level change out of
# ohl_campaign::STARTMAP ("c0a0") lands in, walked from its arrival point.
#
# Project-authored; no bytes here come from any game installation (see
# docs/CLEAN_ROOM.md). Named by its position in the chain rather than by
# the map it runs on, for the reason recorded in c0a0-hop1.txt and in
# xtask/src/chain_walk.rs's module documentation.
#
# A line's leading number is a count of *ticks*, each one CAPTURE_STEP
# (1/60 s) of simulated time — not seconds.
#
# Route, in words only (no entity name, path or coordinate from any
# payload): the player presses nothing at all, because there is nothing to
# press — they arrive aboard the ride and stay aboard it. A per-tick probe
# puts them on a `func_tracktrain` for 3260 of this route's 3261 ticks (the
# missing one is the level-change tick itself); the ride carries them to
# the end of this section and fires the next level change by name, which is
# where `run_chained` ends the route.
#
# This route used to be a bystander's: the arrival placed the player beside
# the ride, which drove off without them, and they stood on world geometry
# for the whole 54 seconds. docs/MILESTONES.md M9.24 read that as an
# arrival fault and M9.25 fixed it — a rider's seat now crosses relative to
# the ride rather than as a raw landmark offset.
#
# The trailing wait is slack: `run_chained` (crates/ohl-app/src/game_run.rs)
# ends a route at the level change it reaches, so any ticks left over here
# are never run.

9000 wait
